/**
 * @gogo/tokens — design tokens + theme definitions (re-skinnable).
 *
 * Scaffold placeholder only: ramps, scales, semantic mapping, and the three
 * accent themes land in P-4 per .specs/design-system/tokens.spec.md.
 * Platform-agnostic by design — no React/RN imports.
 */
export const TOKENS_PACKAGE_NAME = "@gogo/tokens";
