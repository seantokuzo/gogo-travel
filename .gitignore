# Dependencies
node_modules/

# Env / secrets
.env
.env.*
# .env.test is GENERATED throwaway material (scripts/gen-test-env.mjs) —
# explicitly listed so a future narrowing of `.env.*` can't expose it.
.env.test
!.env.example

# Build output
dist/
build/
.expo/
coverage/

# Runtime state
.tmp/
.loop/
*.log
.turbo/
*.tsbuildinfo

# OS / editor
.DS_Store
.idea/

# Claude local
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
